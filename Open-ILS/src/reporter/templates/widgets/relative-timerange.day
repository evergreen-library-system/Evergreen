[%

PROCESS inputs;

WRAPPER select name=input_prefix;
	count = 1;
	def = 1;
	IF current_value;
		def = current_value;
	END;
	WHILE count < 366;
		INCLUDE option value=count selected=(count == def ? 1 : 0);
		count = count + 1;
	END;
END;
' days before runtime';

%]
